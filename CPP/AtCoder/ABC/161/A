#include "bits/stdc++.h"
using namespace std;

void solve() {
    int x, y, z;
    cin >> x >> y >> z;
    swap(x, y);
    swap(x, z);
    cout << x << " " << y << " " << z << "\n";
}

signed main()
{    
    cin.tie(nullptr); 
    ios::sync_with_stdio(false); 
    cout << fixed << setprecision(15);     
    solve();
    return 0;
}
