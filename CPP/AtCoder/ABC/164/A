#include "bits/stdc++.h"
using namespace std;

 template<typename T>void out(T value) { cout << value << "\n"; }

void solve() {
    int s, w;
    cin >> s >> w;
    out((s <= w ? "unsafe" : "safe"));
}

signed main()
{    
    solve();
    return 0;
}
